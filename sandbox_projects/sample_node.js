function Node(val) {                                        /* function that creates new Nodes */
    this.value = value;
    this.next = null;
}

function Sll() {                                            /* function that creates a node "Head" (the first node in a sequence) */
    this.head = head
}

mySll = new Sll()

Node1 = new Node(100)

mySll.head = Node1